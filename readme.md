# WOW Global Solutions Inc. Website

## Set-up
Run
```
npm install
composer install
gulp --production
```

Point web server to the generated `public` directory.

Update `services/config/mail.yml` with valid SMTP server settings.